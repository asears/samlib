[package]
name = "samlib"
version = "0.1.0"
edition = "2021"
description = "Rust implementation of samlib messaging and agent framework"
authors = ["agentic"]
license = "MIT"

[dependencies]
crossbeam = "0.8"
tokio = { version = "1.0", features = ["full"] }
parking_lot = "0.12"
thiserror = "1.0"

[dev-dependencies]
criterion = "0.5"
tokio-test = "0.4"
